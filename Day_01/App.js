// this is for when we have only one HTML Tags. 

const heading = React.createElement("h1",{id:"heading"},"Hello React World @"); 

   const root = ReactDOM.createRoot(document.getElementById("root")); 

   root.render(heading);

console.log(heading);


